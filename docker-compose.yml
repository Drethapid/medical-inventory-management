services:
  web:
    build: .
    ports:
      - "7544:8000"
    volumes:
      - .:/app
      - ./inventory.db:/app/inventory.db
    environment:
      - DATABASE_URL=sqlite:///inventory.db
